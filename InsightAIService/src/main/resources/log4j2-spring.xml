<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">

    <Properties>
        <Property name="LOG_PATTERN">
            %d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %logger - %msg%n
        </Property>
        <Property name="LOG_PATH">D:\logs</Property>
    </Properties>

    <Appenders>

        <!-- Console -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>

        <!-- Main File -->
        <RollingFile name="File"
                     fileName="${LOG_PATH}\app.log"
                     filePattern="${LOG_PATH}\app-%d{yyyy-MM-dd}.log.gz">
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1"/>
            </Policies>
        </RollingFile>

        <RollingFile name="ErrorFile"
                     fileName="${LOG_PATH}\error.log"
                     filePattern="${LOG_PATH}\error-%d{yyyy-MM-dd}.log.gz">
            <Filters>
                <ThresholdFilter level="ERROR" onMatch="ACCEPT" onMismatch="DENY"/>
            </Filters>
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1"/>
            </Policies>
        </RollingFile>

        <RollingFile name="SpyFile"
                     fileName="${LOG_PATH}\spy.log"
                     filePattern="${LOG_PATH}\spy-%d{yyyy-MM-dd}.log.gz">
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1"/>
            </Policies>
        </RollingFile>

    </Appenders>

    <Loggers>

        <!-- Application Logs -->
        <Logger name="com.github.issue.analyze" level="debug" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="File"/>
            <AppenderRef ref="ErrorFile"/>
        </Logger>

        <!-- Hibernate SQL Logs -->
        <Logger name="org.hibernate.SQL" level="debug" additivity="false">
            <AppenderRef ref="SpyFile"/>
        </Logger>

        <!-- Hibernate Parameters -->
        <Logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="trace" additivity="false">
            <AppenderRef ref="SpyFile"/>
        </Logger>

        <!-- Spring JDBC (if used) -->
        <Logger name="org.springframework.jdbc.core" level="debug" additivity="false">
            <AppenderRef ref="SpyFile"/>
        </Logger>

        <!-- Root -->
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="File"/>
            <AppenderRef ref="ErrorFile"/>
        </Root>

    </Loggers>

</Configuration>